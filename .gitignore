# Dependencies
node_modules/

# Build artifacts
web/.next/
web/tsconfig.tsbuildinfo
*.tsbuildinfo
dist/

# Cache archives (keep latest.json, drop timestamped copies)
cache/snapshot-*.json

# OS files
.DS_Store
Thumbs.db

# Env
.env
.env.local

# Local editor/agent state
.zcode/

# Bun lockfile (this repo IS the package; no lockfile needed here)
bun.lock
